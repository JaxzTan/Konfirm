"use client";

import { LinkResultPanel } from "../../LinkResultPanel";

/** Link mode's own result screen — VirusTotal only, no AI claim verdict. */
export default function LinkResultPage() {
  return <LinkResultPanel />;
}
